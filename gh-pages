npm run build
git checkout --orphan gh-pages
git --work-tree=dist add --all    # or build
git --work-tree=dist commit -m "gh-pages deploy"
git push origin gh-pages --force
git checkout -    # back to your previous branch
